import { ModulePlaceholder } from "@/components/features/module-placeholder";

export default function InventoryPage() {
  return (
    <ModulePlaceholder
      title="Inventory"
      phase="Phase W8"
      description="Register objects, assign locations, and search them — the “where is my HDMI cable?” module."
    />
  );
}
