import { ModulePlaceholder } from "@/components/features/module-placeholder";

export default function NowPage() {
  return (
    <ModulePlaceholder
      title="Now"
      phase="Phase W14"
      description="Answers “what should I do now?” by combining tasks, timeline, and memory context through NexusBrain. Flagship feature — implemented last, once every module it draws on exists."
    />
  );
}
